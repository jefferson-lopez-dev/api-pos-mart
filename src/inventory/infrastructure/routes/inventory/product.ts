import { Router } from "express";

export const Product = Router();

Product.get("/product", () => {});
Product.post("/product", () => {});
Product.put("/product", () => {});
Product.delete("/product", () => {});
